@model Ux.Mvc.Web.UI.Icon
@if (!Model.CssClass.IsNullOrEmpty())
{
    <i class="fa fa-@Model.CssClass"  @Model.GetClientIdString() @if(Model.Popover != null) {
                                                                   @Model.Popover.ToHtmlString() 
    }></i>
}